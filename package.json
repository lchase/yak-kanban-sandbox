{
  "name": "yak-kanban-sandbox",
  "version": "0.0.0",
  "private": true,
  "description": "Deliberately-broken kanban board — acceptance sandbox for the yak implement-change workflow and the yak-harness tick loop",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:bugs": "vitest run --config vitest.bugs.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
